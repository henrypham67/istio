apiVersion: v1
kind: Service
metadata:
  name: test-app
  labels:
    app: test-app
    purpose: testing
spec:
  selector:
    app: test-app
  ports:
    - port: 80
      targetPort: 8000
      name: metrics
